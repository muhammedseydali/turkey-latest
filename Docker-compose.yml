version: '3.8'

services:
  django:
    image: turkeybrand
    ports:
      - 8000:8000
    volumes:
      - .:/app
      
  nginx:
    image: nginx:1.21.1
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf